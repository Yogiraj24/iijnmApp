/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.edu.iijnm.jdbc;

import com.edu.iijnm.dao.*;
import com.edu.iijnm.factory.*;
import java.util.Date;
import com.edu.iijnm.dto.*;
import com.edu.iijnm.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class WorkExperienceDaoImpl extends AbstractDAO implements WorkExperienceDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT id, student_id, location_address, org_name, job_nature, org_designation, org_phone, created_date, modified_date FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( id, student_id, location_address, org_name, job_nature, org_designation, org_phone, created_date, modified_date ) VALUES ( ?, ?, ?, ?, ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET id = ?, student_id = ?, location_address = ?, org_name = ?, job_nature = ?, org_designation = ?, org_phone = ?, created_date = ?, modified_date = ? WHERE id = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE id = ?";

	/** 
	 * Index of column id
	 */
	protected static final int COLUMN_ID = 1;

	/** 
	 * Index of column student_id
	 */
	protected static final int COLUMN_STUDENT_ID = 2;

	/** 
	 * Index of column location_address
	 */
	protected static final int COLUMN_LOCATION_ADDRESS = 3;

	/** 
	 * Index of column org_name
	 */
	protected static final int COLUMN_ORG_NAME = 4;

	/** 
	 * Index of column job_nature
	 */
	protected static final int COLUMN_JOB_NATURE = 5;

	/** 
	 * Index of column org_designation
	 */
	protected static final int COLUMN_ORG_DESIGNATION = 6;

	/** 
	 * Index of column org_phone
	 */
	protected static final int COLUMN_ORG_PHONE = 7;

	/** 
	 * Index of column created_date
	 */
	protected static final int COLUMN_CREATED_DATE = 8;

	/** 
	 * Index of column modified_date
	 */
	protected static final int COLUMN_MODIFIED_DATE = 9;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 9;

	/** 
	 * Index of primary-key column id
	 */
	protected static final int PK_COLUMN_ID = 1;

	/** 
	 * Inserts a new row in the work_experience table.
	 */
	public WorkExperiencePk insert(WorkExperience dto) throws WorkExperienceDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			StringBuffer values = new StringBuffer();
			sql.append( "INSERT INTO " + getTableName() + " (" );
			int modifiedCount = 0;
			if (dto.isIdModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "id" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isStudentIdModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "student_id" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isLocationAddressModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "location_address" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isOrgNameModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "org_name" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isJobNatureModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "job_nature" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isOrgDesignationModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "org_designation" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isOrgPhoneModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "org_phone" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isCreatedDateModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "created_date" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isModifiedDateModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "modified_date" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (modifiedCount==0) {
				// nothing to insert
				throw new IllegalStateException( "Nothing to insert" );
			}
		
			sql.append( ") VALUES (" );
			sql.append( values );
			sql.append( ")" );
			stmt = conn.prepareStatement( sql.toString(), Statement.RETURN_GENERATED_KEYS );
			int index = 1;
			if (dto.isIdModified()) {
				stmt.setInt( index++, dto.getId() );
			}
		
			if (dto.isStudentIdModified()) {
				if (dto.isStudentIdNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getStudentId() );
				}
		
			}
		
			if (dto.isLocationAddressModified()) {
				stmt.setString( index++, dto.getLocationAddress() );
			}
		
			if (dto.isOrgNameModified()) {
				stmt.setString( index++, dto.getOrgName() );
			}
		
			if (dto.isJobNatureModified()) {
				stmt.setString( index++, dto.getJobNature() );
			}
		
			if (dto.isOrgDesignationModified()) {
				stmt.setString( index++, dto.getOrgDesignation() );
			}
		
			if (dto.isOrgPhoneModified()) {
				stmt.setString( index++, dto.getOrgPhone() );
			}
		
			if (dto.isCreatedDateModified()) {
				stmt.setTimestamp(index++, dto.getCreatedDate()==null ? null : new java.sql.Timestamp( dto.getCreatedDate().getTime() ) );
			}
		
			if (dto.isModifiedDateModified()) {
				stmt.setTimestamp(index++, dto.getModifiedDate()==null ? null : new java.sql.Timestamp( dto.getModifiedDate().getTime() ) );
			}
		
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		
			// retrieve values from auto-increment columns
			rs = stmt.getGeneratedKeys();
			if (rs != null && rs.next()) {
				dto.setId( rs.getInt( 1 ) );
			}
		
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new WorkExperienceDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the work_experience table.
	 */
	public void update(WorkExperiencePk pk, WorkExperience dto) throws WorkExperienceDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			sql.append( "UPDATE " + getTableName() + " SET " );
			boolean modified = false;
			if (dto.isIdModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "id=?" );
				modified=true;
			}
		
			if (dto.isStudentIdModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "student_id=?" );
				modified=true;
			}
		
			if (dto.isLocationAddressModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "location_address=?" );
				modified=true;
			}
		
			if (dto.isOrgNameModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "org_name=?" );
				modified=true;
			}
		
			if (dto.isJobNatureModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "job_nature=?" );
				modified=true;
			}
		
			if (dto.isOrgDesignationModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "org_designation=?" );
				modified=true;
			}
		
			if (dto.isOrgPhoneModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "org_phone=?" );
				modified=true;
			}
		
			if (dto.isCreatedDateModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "created_date=?" );
				modified=true;
			}
		
			if (dto.isModifiedDateModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "modified_date=?" );
				modified=true;
			}
		
			if (!modified) {
				// nothing to update
				return;
			}
		
			sql.append( " WHERE id=?" );
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			stmt = conn.prepareStatement( sql.toString() );
			int index = 1;
			if (dto.isIdModified()) {
				stmt.setInt( index++, dto.getId() );
			}
		
			if (dto.isStudentIdModified()) {
				if (dto.isStudentIdNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getStudentId() );
				}
		
			}
		
			if (dto.isLocationAddressModified()) {
				stmt.setString( index++, dto.getLocationAddress() );
			}
		
			if (dto.isOrgNameModified()) {
				stmt.setString( index++, dto.getOrgName() );
			}
		
			if (dto.isJobNatureModified()) {
				stmt.setString( index++, dto.getJobNature() );
			}
		
			if (dto.isOrgDesignationModified()) {
				stmt.setString( index++, dto.getOrgDesignation() );
			}
		
			if (dto.isOrgPhoneModified()) {
				stmt.setString( index++, dto.getOrgPhone() );
			}
		
			if (dto.isCreatedDateModified()) {
				stmt.setTimestamp(index++, dto.getCreatedDate()==null ? null : new java.sql.Timestamp( dto.getCreatedDate().getTime() ) );
			}
		
			if (dto.isModifiedDateModified()) {
				stmt.setTimestamp(index++, dto.getModifiedDate()==null ? null : new java.sql.Timestamp( dto.getModifiedDate().getTime() ) );
			}
		
			stmt.setInt( index++, pk.getId() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new WorkExperienceDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the work_experience table.
	 */
	public void delete(WorkExperiencePk pk) throws WorkExperienceDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getId() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new WorkExperienceDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the work_experience table that matches the specified primary-key value.
	 */
	public WorkExperience findByPrimaryKey(WorkExperiencePk pk) throws WorkExperienceDaoException
	{
		return findByPrimaryKey( pk.getId() );
	}

	/** 
	 * Returns all rows from the work_experience table that match the criteria 'id = :id'.
	 */
	public WorkExperience findByPrimaryKey(int id) throws WorkExperienceDaoException
	{
		WorkExperience ret[] = findByDynamicSelect( SQL_SELECT + " WHERE id = ?", new Object[] {  new Integer(id) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the work_experience table that match the criteria ''.
	 */
	public WorkExperience[] findAll() throws WorkExperienceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY id", null );
	}

	/** 
	 * Returns all rows from the work_experience table that match the criteria 'student_id = :studentId'.
	 */
	public WorkExperience[] findByPersonalDetails(int studentId) throws WorkExperienceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE student_id = ?", new Object[] {  new Integer(studentId) } );
	}

	/** 
	 * Returns all rows from the work_experience table that match the criteria 'id = :id'.
	 */
	public WorkExperience[] findWhereIdEquals(int id) throws WorkExperienceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE id = ? ORDER BY id", new Object[] {  new Integer(id) } );
	}

	/** 
	 * Returns all rows from the work_experience table that match the criteria 'student_id = :studentId'.
	 */
	public WorkExperience[] findWhereStudentIdEquals(int studentId) throws WorkExperienceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE student_id = ? ORDER BY student_id", new Object[] {  new Integer(studentId) } );
	}

	/** 
	 * Returns all rows from the work_experience table that match the criteria 'location_address = :locationAddress'.
	 */
	public WorkExperience[] findWhereLocationAddressEquals(String locationAddress) throws WorkExperienceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE location_address = ? ORDER BY location_address", new Object[] { locationAddress } );
	}

	/** 
	 * Returns all rows from the work_experience table that match the criteria 'org_name = :orgName'.
	 */
	public WorkExperience[] findWhereOrgNameEquals(String orgName) throws WorkExperienceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE org_name = ? ORDER BY org_name", new Object[] { orgName } );
	}

	/** 
	 * Returns all rows from the work_experience table that match the criteria 'job_nature = :jobNature'.
	 */
	public WorkExperience[] findWhereJobNatureEquals(String jobNature) throws WorkExperienceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE job_nature = ? ORDER BY job_nature", new Object[] { jobNature } );
	}

	/** 
	 * Returns all rows from the work_experience table that match the criteria 'org_designation = :orgDesignation'.
	 */
	public WorkExperience[] findWhereOrgDesignationEquals(String orgDesignation) throws WorkExperienceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE org_designation = ? ORDER BY org_designation", new Object[] { orgDesignation } );
	}

	/** 
	 * Returns all rows from the work_experience table that match the criteria 'org_phone = :orgPhone'.
	 */
	public WorkExperience[] findWhereOrgPhoneEquals(String orgPhone) throws WorkExperienceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE org_phone = ? ORDER BY org_phone", new Object[] { orgPhone } );
	}

	/** 
	 * Returns all rows from the work_experience table that match the criteria 'created_date = :createdDate'.
	 */
	public WorkExperience[] findWhereCreatedDateEquals(Date createdDate) throws WorkExperienceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE created_date = ? ORDER BY created_date", new Object[] { createdDate==null ? null : new java.sql.Timestamp( createdDate.getTime() ) } );
	}

	/** 
	 * Returns all rows from the work_experience table that match the criteria 'modified_date = :modifiedDate'.
	 */
	public WorkExperience[] findWhereModifiedDateEquals(Date modifiedDate) throws WorkExperienceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE modified_date = ? ORDER BY modified_date", new Object[] { modifiedDate==null ? null : new java.sql.Timestamp( modifiedDate.getTime() ) } );
	}

	/**
	 * Method 'WorkExperienceDaoImpl'
	 * 
	 */
	public WorkExperienceDaoImpl()
	{
	}

	/**
	 * Method 'WorkExperienceDaoImpl'
	 * 
	 * @param userConn
	 */
	public WorkExperienceDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "iijnmdb.work_experience";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected WorkExperience fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			WorkExperience dto = new WorkExperience();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected WorkExperience[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			WorkExperience dto = new WorkExperience();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		WorkExperience ret[] = new WorkExperience[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(WorkExperience dto, ResultSet rs) throws SQLException
	{
		dto.setId( rs.getInt( COLUMN_ID ) );
		dto.setStudentId( rs.getInt( COLUMN_STUDENT_ID ) );
		if (rs.wasNull()) {
			dto.setStudentIdNull( true );
		}
		
		dto.setLocationAddress( rs.getString( COLUMN_LOCATION_ADDRESS ) );
		dto.setOrgName( rs.getString( COLUMN_ORG_NAME ) );
		dto.setJobNature( rs.getString( COLUMN_JOB_NATURE ) );
		dto.setOrgDesignation( rs.getString( COLUMN_ORG_DESIGNATION ) );
		dto.setOrgPhone( rs.getString( COLUMN_ORG_PHONE ) );
		dto.setCreatedDate( rs.getTimestamp(COLUMN_CREATED_DATE ) );
		dto.setModifiedDate( rs.getTimestamp(COLUMN_MODIFIED_DATE ) );
		reset(dto);
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(WorkExperience dto)
	{
		dto.setIdModified( false );
		dto.setStudentIdModified( false );
		dto.setLocationAddressModified( false );
		dto.setOrgNameModified( false );
		dto.setJobNatureModified( false );
		dto.setOrgDesignationModified( false );
		dto.setOrgPhoneModified( false );
		dto.setCreatedDateModified( false );
		dto.setModifiedDateModified( false );
	}

	/** 
	 * Returns all rows from the work_experience table that match the specified arbitrary SQL statement
	 */
	public WorkExperience[] findByDynamicSelect(String sql, Object[] sqlParams) throws WorkExperienceDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new WorkExperienceDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the work_experience table that match the specified arbitrary SQL statement
	 */
	public WorkExperience[] findByDynamicWhere(String sql, Object[] sqlParams) throws WorkExperienceDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new WorkExperienceDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
